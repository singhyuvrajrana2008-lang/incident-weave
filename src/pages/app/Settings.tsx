import { useState } from "react";
import { Page } from "../../components/shell/Page";
import { Button } from "../../components/ui";
export default function Settings(){const [dense,setDense]=useState(false);const [review,setReview]=useState(true);return <Page eyebrow="Account" title="Settings" subtitle="Workspace preferences for investigation review."><div className="max-w-2xl overflow-hidden rounded-lg border border-line bg-surface">{[["Dense evidence rows","Show more source metadata in compact lists",dense,setDense],["Require human review","Keep AI findings in review state until acknowledged",review,setReview]].map(([title,text,val,setter])=><label key={title as string} className="flex cursor-pointer items-center justify-between gap-6 border-b border-line p-5 last:border-0"><div><div className="font-display font-medium">{title as string}</div><div className="mt-1 text-sm text-fg-dim">{text as string}</div></div><input type="checkbox" checked={Boolean(val)} onChange={e=>(setter as (v:boolean)=>void)(e.target.checked)} className="size-4 accent-[var(--color-accent)]"/></label>)}</div><Button variant="outline" className="mt-5">Preferences are stored locally in this demo</Button></Page>}
